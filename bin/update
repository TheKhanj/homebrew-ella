#!/usr/bin/env sh

ellarb="Formula/ella.rb"

make -B &&
	git add "$ellarb" && {
	version="$(
		cat "$ellarb" |
			grep -oP "(?<=version \")([^\"]*)"
	)"
	git commit -m "bot: update to version v${version}"
}
